{% extends "base.njk" %}

{% block main %}
  <h1>All posts that are tagged with <span class="text-primary">{{ tag }}<span></h1>
  {% for post in search.pages(tag, "date=desc") %}
    {{ comp.entry({ post: post }) | safe }}
  {% endfor %}
{% endblock %}